# Copy to `.env` to point the UI at a backend other than the local default.
# Vite only exposes variables prefixed with VITE_ to the browser — never put a
# secret in this file or in `.env`, because everything here is bundled and shipped.
VITE_API_BASE_URL=http://localhost:8000
